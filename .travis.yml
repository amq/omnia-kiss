language: cpp
sudo: false

cache: ccache

script:
  - \cp -r custom lede
  - cd lede
  - ./scripts/feeds update -a
  - ./scripts/feeds install -a
  - make defconfig
  - make V=s | egrep -i '(warn|error)'
