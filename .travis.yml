language: cpp
sudo: false

cache: ccache

script:
  - cp -rf custom/* lede
  - cd lede
  - cat .config
  - ./scripts/feeds update -a
  - ./scripts/feeds install -a
  - make defconfig
  - make V=s | egrep -i '(warn|error)'
